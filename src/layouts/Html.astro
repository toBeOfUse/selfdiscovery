<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/sine.png" />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <meta name="generator" content={Astro.generator} />
    <slot name="head" />
  </head>
  <body>
    <slot />
  </body>
</html>

<script>
  // this halts animated svgs if the user has operating system settings that
  // disable animations

  let reducedMotion = window.matchMedia(`(prefers-reduced-motion: reduce)`).matches === true;

  if (reducedMotion) {
    stopSmilAnimations();
  }

  function stopSmilAnimations() {
    let svgs = document.querySelectorAll("svg");
    svgs.forEach((svg) => {
      svg.pauseAnimations();
    });
  }
</script>
